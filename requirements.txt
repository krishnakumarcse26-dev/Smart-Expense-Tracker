# ─────────────────────────────────────────────────────────
# requirements.txt
#
# Install all dependencies with:
#   pip install -r requirements.txt
#
# On Render/Railway, this file is read automatically.
# ─────────────────────────────────────────────────────────

# Core Django framework
Django==5.0.6

# Form rendering with Bootstrap 5 styling
django-crispy-forms==2.1
crispy-bootstrap5==2024.2

# Serve static files in production (replaces a static file server)
whitenoise==6.6.0

# WSGI server for production (replaces Django's dev server)
gunicorn==21.2.0

# PostgreSQL adapter (required for production database)
psycopg2-binary==2.9.9

# Read environment variables from .env file
python-decouple==3.8

# Image handling (for profile pictures)
Pillow==10.3.0

# Excel export
openpyxl==3.1.2

# PDF generation
reportlab==4.1.0
